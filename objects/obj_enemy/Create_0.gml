//visible = false;
dir = irandom_range(0,360);          // last known direction (angle)
move_speed = 3;   // movement speed
range = 700;      // detection range
image_speed = 0;
depth = -1000000000
alarm[0] = room_speed *3
ghost_sound = choose(snd_close,snd_close,snd_close,snd_close_load,snd_siren,snd_siren)